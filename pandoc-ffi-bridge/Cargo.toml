[package]
name = "ffi-bridge"
version = "0.1.0"
authors = ["Cormac Relf <web@cormacrelf.net>"]
license = "MPL-2.0"
edition = "2018"
build = "build.rs"

[dependencies]
libc = "0.2.45"
# curryrs = "0.2.0"

# [profile.dev]
# rpath = true
